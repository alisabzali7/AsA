> **HISTORICAL RECORD** of prior cleanup runs. Retained as an audit trail.
> Superseded by `docs/audit/WORKSPACE_SURGERY.md` for the current run.

# CLEANUP MANIFEST — Backend Closure & Freeze

Every deletion was preceded by a reference check (`grep -rl <basename>` over
`src/`, `scripts/`, `tests/`, `docs/`). Files with any live reference were RETAINED
regardless of classification.

**Removed: 23 entries · Retained with proof: 8**

## Removed

| path | classification / reason | bytes | sha256(16) | replacement |
|---|---|---|---|---|
| `.env.local` | SECRET: live TTT/AI/Telegram credentials. Removed from the deliverable; these values MUST be rotated before redeploying. | 3028 | `a0ea572d9f824c86` | .env.example documents required variable NAMES only |
| `uploads/api.txt` | SECRET: credential inventory, not application code. | 321 | `556001ea9c1a513f` | .env.example |
| `tsconfig.tsbuildinfo` | GENERATED build cache; regenerated by `npm run typecheck`. | 145749 | `67d8a06561ea324b` | — |
| `uploads/package.json` | DUPLICATE: byte-mirror of the real project file at the repo root. | 1021 | `37f793882b47549f` | repo-root equivalent |
| `uploads/package-lock.json` | DUPLICATE: byte-mirror of the real project file at the repo root. | 298678 | `a1a33ccf4f5281fe` | repo-root equivalent |
| `uploads/eslint.config.mjs` | DUPLICATE: byte-mirror of the real project file at the repo root. | 365 | `c971926abccc506d` | repo-root equivalent |
| `uploads/postcss.config.mjs` | DUPLICATE: byte-mirror of the real project file at the repo root. | 108 | `b8cd36495468ea54` | repo-root equivalent |
| `uploads/tsconfig.json` | DUPLICATE: byte-mirror of the real project file at the repo root. | 723 | `4ea5303fb21d01b8` | repo-root equivalent |
| `uploads/README.md` | DUPLICATE: byte-mirror of the real project file at the repo root. | — | `-` | repo-root equivalent |
| `uploads/CHANGELOG.md` | DUPLICATE: byte-mirror of the real project file at the repo root. | — | `-` | repo-root equivalent |
| `uploads/ASA_UNIVERSE.json` | DUPLICATE: verified byte-identical 48-symbol set to src/lib/domain/universe.ts (TONUSDT absent in both). | 1024 | `-` | src/lib/domain/universe.ts (canonical) |
| `uploads/strategy_registry.csv` | DUPLICATE: static mirror of the Brain registry now served by /api/brain/strategies. | 12000 | `-` | brain.db + /api/brain/strategies |
| `uploads/STRATEGY_EVIDENCE.txt` | DUPLICATE: provenance already persisted per-strategy in brain.db source_refs. | 15000 | `-` | brain.db source_refs |
| `uploads/ASA_MASTER_PROMPT_README.txt` | AGENT_HANDOFF_ARTIFACT: superseded prompt, not a runtime input. Identity archived in docs/archive/SUPERSEDED_PROMPTS.md. | — | `-` | docs/archive/SUPERSEDED_PROMPTS.md |
| `uploads/ASA_MASTER_PROMPT_STRATEGY_RISK_PSYCHOLOGY_BRAIN_v2-1.txt` | AGENT_HANDOFF_ARTIFACT: superseded prompt, not a runtime input. Identity archived in docs/archive/SUPERSEDED_PROMPTS.md. | — | `-` | docs/archive/SUPERSEDED_PROMPTS.md |
| `uploads/ASA_SUPER_MASTER_DEVELOPMENT_PROMPT_v6_FINAL.txt` | AGENT_HANDOFF_ARTIFACT: superseded prompt, not a runtime input. Identity archived in docs/archive/SUPERSEDED_PROMPTS.md. | — | `-` | docs/archive/SUPERSEDED_PROMPTS.md |
| `uploads/ASA_AGENT_EXTREME_BENCHMARK_PROMPT_v4_FINAL.txt` | AGENT_HANDOFF_ARTIFACT: superseded prompt, not a runtime input. Identity archived in docs/archive/SUPERSEDED_PROMPTS.md. | — | `-` | docs/archive/SUPERSEDED_PROMPTS.md |
| `uploads/MASTER_PROMPT_ASA_AGENT_EXECUTION_v1.txt` | AGENT_HANDOFF_ARTIFACT: superseded prompt, not a runtime input. Identity archived in docs/archive/SUPERSEDED_PROMPTS.md. | — | `-` | docs/archive/SUPERSEDED_PROMPTS.md |
| `uploads/Fable_5_1_AsA_Frontend_Master_Prompt.txt` | AGENT_HANDOFF_ARTIFACT: superseded prompt, not a runtime input. Identity archived in docs/archive/SUPERSEDED_PROMPTS.md. | — | `-` | docs/archive/SUPERSEDED_PROMPTS.md |
| `uploads/FINAL_AI_TRADE_DEEP_RESEARCH_REPORT.txt` | AGENT_HANDOFF_ARTIFACT: superseded prompt, not a runtime input. Identity archived in docs/archive/SUPERSEDED_PROMPTS.md. | — | `-` | docs/archive/SUPERSEDED_PROMPTS.md |
| `src/lib/backtest/engine.ts (456-line duplicate loop)` | DEAD_CODE: second strategy-evaluation implementation with its own bar loop, hard-coded 15m timeframe and slice-relative exit indexes. Replaced by a thin adapter over the shared runner. | — | `-` | src/lib/backtest/strategy-runner.ts |
| `src/lib/strategy/reference.ts (production path)` | LEGACY: referenceStrategy removed from the production registry; retained ONLY as a test harness behind getReferenceHarness(). | — | `-` | src/lib/strategy/runtime.ts (Brain-backed) |
| `transient .asa-ingest-* / .asa-val-* / probe .cjs artifacts` | GENERATED transient build artifacts from script runs; now gitignored and cleaned automatically. | — | `-` | — |

## Retained (deletion refused without proof)

| path | reason |
|---|---|
| `uploads/RAW_1.txt … RAW_5.txt` | IMMUTABLE SOURCE CORPUS — ingestion + provenance input; sha256 verified unchanged after cleanup |
| `uploads/ASA_CANONICAL_KNOWLEDGE_PACK_v1_1.json` | Deterministic ingest/audit input (authority #2) |
| `uploads/API reference _ TheTrueTrade(1).PDF` | Vendor reference for TTT capability verification |
| `uploads/AsA_Master_Build_Bible_v1.0.pdf` | Source-of-requirements artifact |
| `uploads/ASA_IMPLEMENTATION_CONTRACT_v1.md` | Active product/data/runtime contract |
| `uploads/00_READ_FIRST.md, PACKAGE_MANIFEST.txt` | Package orientation/manifest |
| `tests/fixtures/replay/*.json` | Real TTT replay data (12 series, 9 symbols) required for reproducible backtests |
| `src/lib/strategy/reference.ts` | RETAINED as a test-only harness; excluded from every production listing |

## Post-cleanup verification

- `npm run brain:ingest` → 9,398 fragments, `coverage_ok: true`, all five RAW sha256 **unchanged**
- `npm run brain:validate` → 152 experiments persisted append-only across 9 symbols
- `npx tsc --noEmit` clean · `npx eslint .` clean · `npm test` 270/270 · `npm run build` clean
- live chart route served JSON evidence + a valid 900×500 PNG + lineage-tagged SVG

## Secret scan

- Working tree: **NONE** (224 files scanned; `.env.example` holds variable NAMES with empty values)
- Full git history: **NONE** (13 commits, every blob scanned)
- `.env.local` and `uploads/api.txt` are absent from the deliverable

## CREDENTIAL ROTATION REQUIRED

`.env.local` contained live TTT, AI and Telegram credentials. **Rotate the TTT API
key/secret, the AI provider key and the Telegram bot token before redeploying.**

---

## Market-data remediation (this run)

### Replaced / superseded (behaviour changed in place, code retained)

| path | change | reason | replacement |
|---|---|---|---|
| `src/lib/domain/universe.ts` | `UNIVERSE` -> `LEGACY_UNIVERSE` | the fixed 48-symbol list is no longer the universe; it is now a permanent REGRESSION SET | `src/lib/market/catalog.ts` (dynamic TTT discovery) |
| `src/lib/market/candles.ts` | in-memory trim now persists first | `slice(-target)` was silently discarding history | `src/lib/market/history-store.ts` (durable retention) |
| `src/app/api/charts/[id]/route.ts` | removed `candles.slice(-400)` | a fixed render window was acting as a historical ceiling | full stored range + optional `bars`/`from`/`to` transport window |
| `src/app/api/research/backtest/route.ts` | removed `Math.min(5000, ...)` bar cap and `candleManager` dependency | backtest was capped at the venue response size | shared `syncHistory` + `getHistoryStore` (§16) |
| `src/app/api/market/candles/route.ts` | `limit` documented as a TRANSPORT window | it was ambiguous whether 700 was a retention limit | `/api/market/history` for full-range traversal |

### Removed

| path | reason | reference check |
|---|---|---|
| `tsconfig.tsbuildinfo` | GENERATED build cache | regenerated by `npm run typecheck` |
| `.asa-ingest-*`, `.asa-val-*`, probe `.cjs` | GENERATED transient script artifacts | gitignored; no references |

### Retained (deletion refused)

| path | reason |
|---|---|
| `src/lib/domain/universe.ts` | still the synchronous guard + the 48-symbol regression set |
| `src/lib/market/candles.ts` | still the live hot-window manager for the board/scanner |
| `tests/fixtures/replay/*.json` | deterministic offline replay fixtures |

### New modules

`src/lib/market/catalog.ts` · `src/lib/market/history.ts` · `src/lib/market/history-store.ts`
`src/app/api/market/catalog` · `/history` · `/matrix` · `/sync-status`

# Workspace Surgery — cleanup record

Cleanup only. No architecture, strategy, Brain, scoring, risk or chart changes.

## Before → After

| Metric | Before | After |
|---|---|---|
| Workspace (excl. node_modules) | 197.2 MB | **8.6 MB** |
| Source footprint | 20.6 MB* | **8.6 MB** |
| Files (excl. node_modules) | 1,544 | 652 → 377 |
| Source handoff ZIP | 2.20 MB (nested in project) | **2.20 MB (outside project)** |

\* after `_verify` removal but before `asa-data`/cache purge.

## Deleted — largest first

| Item | Size | Files | Reason |
|---|---|---|---|
| `node_modules/.npm` cache | 185.0 MB | 894 | npm package cache; reproducible |
| `.next/` | 125.7 MB | — | build output; regenerated by `npm run build` |
| `asa-data/history.db(+wal/shm)` | 85.2 MB | 3 | rebuildable: proven 6,769 bars re-synced from TTT |
| `_verify/` | 21.3 MB | 275 | verification snapshot; 267/272 byte-identical to root, 0 unique files, 0 references |
| `asa-data/brain.db(+wal/shm)` | 13.3 MB | 3 | rebuildable: proven 9,398 fragments / 8,591 atoms |
| `asa-source-handoff.zip` | 2.2 MB | 1 | nested archive inside the project |
| `asa-data/asa.db(+wal/shm)` | 1.0 MB | 3 | runtime state; recreated at boot |
| `tsconfig.tsbuildinfo` | 160 KB | 1 | incremental build cache |
| `asa-source-handoff.zip.sha256` | 89 B | 1 | belongs with the archive, outside the tree |

## Retained large files — why

| Item | Size | Why it must stay |
|---|---|---|
| `knowledge/raw/RAW_1..5.txt` | 2.4 MB | **immutable source corpus** — the primary intellectual asset |
| `knowledge/canonical/…pack.json` | 1.3 MB | deterministic ingest/audit input |
| `.git/` | 12.2 MB | repository history (workspace only; excluded from handoff) |
| `tests/fixtures/replay/*.json` | 1.4 MB | real TTT replay data for reproducible backtests |
| `docs/ttt/*.pdf`, `docs/archive/*.pdf` | 2.0 MB | vendor API reference + source-of-requirements |

## Duplicate scan

SHA256 across the whole tree found **one** pair: `auth-orders-401.json` and
`auth-positions-401.json` (58 bytes each). They document **different endpoints**
returning identical 401 bodies — evidentiary, retained deliberately.

## Rebuild proofs

- **Brain:** deleted `brain.db` → `brain:ingest` + `brain:mine` reproduced
  9,398 fragments, 8,591 atoms, 621 components, 55 candidates. **PASS**
- **History:** deleted `history.db` → `syncHistory('BTCUSDT','1h')` rebuilt
  6,769 bars, `COMPLETE_TO_TTT_BOUNDARY`, earliest 2025-12-03. **PASS**

## Not fixed by this run (out of scope, documented)

1. `experiments` table is **not** regenerated by ingest/mine — the 152
   OOS/walk-forward rows were lost in an earlier rebuild. `npm run brain:validate`
   recreates them.
2. The Brain rule registry and the runtime rule set remain separate systems.

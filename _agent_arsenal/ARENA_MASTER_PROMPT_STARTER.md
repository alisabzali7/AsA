[Agent Mode — Use the best available orchestrator for this mission. Read the repository `AGENTS.md` and relevant `.agents/skills/` before execution. Use Arena-native tools and connected integrations that are actually available. Do not claim unavailable MCP/Skills are active.]

# MISSION

Task:
[PUT THE LARGE MISSION HERE]

## Execution contract

Execute this as one coherent end-to-end engineering mission.

Do not stop after analysis, planning, scaffolding, or a partial patch.

Inspect the current repository first.

Use the relevant AsA skills.

Use external documentation/tools when they materially improve correctness.

Implement as much of the mission as the repository and actual capabilities permit.

Run tests.
Run the real application when applicable.
Use browser/computer verification for UI tasks when available.
Review your own diff.

Commit and open/update the session PR when the task reaches a coherent reviewable state.

## AsA truth rules

- TTT is the production market truth.
- No hidden mock fallback.
- Unknown remains Unknown.
- User-authored methodology is authoritative.
- Never invent Strategy or Psychology semantics.
- LLM explanations cannot override deterministic AsA decisions.
- Never fabricate verification.

## Required final report

1. EXECUTIVE RESULT
2. ACTUAL CHANGES
3. SOURCE EVIDENCE USED
4. TESTS ACTUALLY RUN
5. RUNTIME/BROWSER VERIFICATION
6. REMAINING BLOCKERS
7. GIT/PR RESULT
8. NOT VERIFIED ITEMS
